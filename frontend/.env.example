# Backend origin when the UI is on Vercel (scheme + host only; no trailing /api).
# Recommended:
VITE_API_BASE_URL=

# Alternative (CRA-style name; supported via envPrefix in vite.config.js):
REACT_APP_AI_URL=
